/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  //  devIndicators: false,
  // We're using our own custom locale routing implementation
  // through the [locale] folder structure and middleware
};

module.exports = nextConfig;
